from datetime import date
from projectX.engine.calculation_engine import run_calculation

data = {
    "dob": date(1995, 5, 10),
    "premium": 20000,
    "pt": 15,
    "ppt": 10,
    "frequency": "Yearly",
    "sum_assured": 500000
}

result = run_calculation(data)
print(result)